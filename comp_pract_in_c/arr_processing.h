#pragma once
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int* withoutDuplicates(int* arr, int size, int* newSize); /* возвращаем новый массив без дубликатов */

int sumNegativMultipleOfFour(int* arr, int size); /* сумма негативных чисел кратным 4 */
